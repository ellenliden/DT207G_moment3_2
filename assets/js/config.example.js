// en mall för config.js filen
/* eslint-disable no-unused-vars */
const API_CONFIG = {
  baseUrl: "https://your-api.example.com", // exempel
  itemsPath: "/items", // exempel  /courses ETC beroende på min API
};
